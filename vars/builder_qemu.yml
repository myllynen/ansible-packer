---
# https://www.packer.io/docs/builders/qemu
output_directory: "{{ packer_output | default('/tmp/packer_images') }}"

vm_name: "{{ image_name | default(target_fullname + '_image.qcow2', true) }}"

vm_cpus: 2
vm_memory: 4096
vm_disk_size: "{{ disk_size | default(30720) }}"
